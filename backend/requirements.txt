# Khuyến nghị Python 3.11+ (đã kiểm thử trên 3.14). Dùng '>=' để pip chọn wheel
# tương thích phiên bản Python của bạn. Muốn khóa chặt để tái lập, hãy `pip freeze`.

# --- Core web ---
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7

# --- DB (engine-agnostic; SQLite dev, Postgres prod) ---
SQLAlchemy>=2.0.36
psycopg[binary]>=3.2          # driver Postgres (không dùng khi chạy SQLite)
alembic>=1.14                 # schema migrations (alembic upgrade head)

# --- SQL parsing / guards ---
sqlparse>=0.5

# --- Semantic layer ---
PyYAML>=6.0

# --- LLM client (OpenAI-compatible: key + model + base_url) ---
openai>=1.59

# --- Mock data / thống kê (reporter) ---
numpy>=2.1
pandas>=2.2

# --- Dev / test ---
pytest>=8.3
httpx>=0.28
